
import { StyleSheet } from 'react-native';
import { TABLE_WIDTH, TABLE_HEIGHT } from 'src/constants/sizes';

const styles = StyleSheet.create({
  table: {
    width: TABLE_WIDTH,
    height: TABLE_HEIGHT,
  },
  row: {
    flexDirection: 'row',
  },
});

export default styles;
